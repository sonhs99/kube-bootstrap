apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: proxmox-internal-route
  namespace: external
spec:
  parentRefs:
  - group: gateway.networking.k8s.io	
    kind: Gateway	
    name: gateway-internal
    namespace: default
    sectionName: https
  hostnames: ["pve.internal.lazydogs.work"]
  rules:
  - backendRefs:
      - group: ''	
        kind: Service
        name: proxmox
        port: 443
        weight: 1
    matches:
      - path:
          type: PathPrefix
          value: /
---
apiVersion: gateway.networking.k8s.io/v1
kind: BackendTLSPolicy
metadata:
  name: proxmox-backend-tls-policy
  namespace: external
spec:
  targetRefs:
    - group: ""
      kind: Service
      name: proxmox
  validation:
    hostname: pve01
    caCertificateRefs:
      - group: ""
        kind: ConfigMap
        name: proxmox-ca