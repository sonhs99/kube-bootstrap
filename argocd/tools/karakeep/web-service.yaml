apiVersion: v1
kind: Service
metadata:
  name: karakeep-web
  namespace: tools
spec:
  selector:
    app: karakeep-web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP