apiVersion: v1
kind: Service
metadata:
  name: rest-server
  namespace: restic
  annotations:
    argocd.argoproj.io/sync-wave: "-35"
spec:
  ports:
  - port: 80
    targetPort: 8000
  selector:
    app: rest-server